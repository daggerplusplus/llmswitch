# Server Configuration
# The host/IP address the server will bind to
# Use 0.0.0.0 to listen on all network interfaces (recommended for Docker)
# Use localhost or 127.0.0.1 to listen only on localhost
SERVER_HOST=0.0.0.0

# Server port
PORT=3000

# Ollama Configuration
# The host/IP where Ollama is running
OLLAMA_HOST=localhost

# The port where Ollama is listening
OLLAMA_PORT=11434
